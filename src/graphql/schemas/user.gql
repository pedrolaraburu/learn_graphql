type User {
  id: ID!
  email: String!
  name: String
  password: String!
  createdAt: String!
  updatedAt: String!
  tasks: [Task]
}

type Query {
    users: [User!]!
    user(id: ID!): User
}

type Mutation {
  createUser(email: String!, name: String!, password: String!): User!
}
